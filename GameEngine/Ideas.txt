Режимы и т.п. с помощью глобальных переменных
Time!!!
Камера: создавать surface и блитить на нее все объекты
каждая картинка - поверхность, которую можно менять
Тэги и слои
Renderer
Вкл/выкл кэширование
обновлять элементы
FPS за счет сохранения разницы между кадрами и умножение на дельту тайм
overlapped возвращает коллайдер
базовые фигуры

При self:
	Если vector.y = 1 (other ниже) = откл гравитацию
	Если vector.y = -1 = обнулить velocity.y
	Если vector.x = 1 or vector.x = -1 = обнулить velocity.x
	
	p1 - o.p1, p2 - o.p2, p3 - o.p3, p4 - o.p4
	
	из каждой точки вычесть ту же точку на другом объекте
	
Конвертация картинки pygame (image.convert())
Класс Color, поддерживаемый Renderer помимо списка/кортежа
В рендерере возможность поменять цвет всего изображения на k (коэффициент) к цвету c
Движение камеры
Библиотека для отслеживания скорости работы кода (функций)
GetKey, GetAnyKey, GetSomeKey, GetMouseButton, GetAnyMouseButton, GetSomeMouseButton, GetMousePosition, GetAxis, GetAxisRaw
Поворот коллайдера (отслеживавть вектор по углу поворота)
UI 
Для нахождения любого столкновения коллайдера с определенным тегом/слоем используем filter
Анимации
collider & rigidbody для UI
Коллайдер и rigidbody отдельно
Parents & children
проверяем на столкновение только те объекты, которые относительно рядом